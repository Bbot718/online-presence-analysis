# Local paths
LLAMA_MODEL_PATH=./models/llama-2-7b-chat.gguf

# Optional: Configure model parameters
LLAMA_N_THREADS=4  # Adjust based on your CPU
LLAMA_N_CTX=2048   # Context window size

# Optional: Configure user agent for web scraping
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# Optional: Configure request delays to avoid rate limiting
REQUEST_DELAY=2  # Delay in seconds between requests 